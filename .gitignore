# Root monorepo
.DS_Store

# Do not re-commit Flutter artifacts at repo root (app lives in frontend/)
.dart_tool/
build/

# Renamed legacy caches / outputs (prefer not to re-add if removed from the index)
ZZZ_dart_tool/
ZZZ_build/

# Backend (Python)
backend/.venv/
backend/venv/
backend/**/__pycache__/
backend/**/*.py[cod]
backend/.pytest_cache/
backend/.mypy_cache/
backend/data/*.db
backend/data/*.db-*

# Local tooling / large downloads (keep out of git if added)
tool/*.zip
